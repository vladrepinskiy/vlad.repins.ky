import { styled } from "goober";
import { Body } from "./Typography";

export const MobileCallout = () => {
  return (
    <Body>
      You've landed on the simplified mobile version. Scroll down for CV, but
      visit the desktop version for full experience.
    </Body>
  );
};
